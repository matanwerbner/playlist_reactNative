import Colors from '../../Themes/Colors';

export default {
    btnStyle: {
        alignItems: 'center',
        justifyContent: 'center',
        borderColor: Colors.blue,
        width: 35,
        height: 35
    },
    btnDisabled: {
        opacity: 0.5
    },
    iconStyle: {
        color: Colors.blue
    }
}