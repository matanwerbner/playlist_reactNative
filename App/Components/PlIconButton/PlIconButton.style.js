import Colors from '../../Themes/Colors';

export default {
    btnStyle: {
        alignItems: 'center',
        height: 35,
        width: 35,
        borderColor: Colors.blue,
        justifyContent: 'center'
    },
    btnDisabled: {
        opacity: 0.5
    },
    iconStyle: {
        color: Colors.blue
    }
}