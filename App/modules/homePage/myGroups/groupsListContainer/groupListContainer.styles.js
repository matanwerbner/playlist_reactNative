import { Fonts, Colors } from '../../../../Themes'

export default {
    titleContainer: {
        flexDirection: 'row',
        borderBottomWidth: 1,
        borderBottomColor: 'black',
        alignItems: 'center',
    },
    createGroupText: {
        ...Fonts.style.small
    }
}